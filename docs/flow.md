# Request/Response Flow Diagrams - SYN CLI

**Date:** 2026-01-22

---

## Table of Contents

1. [Application Startup Flow](#application-startup-flow)
2. [One-Shot Chat Flow](#one-shot-chat-flow)
3. [Interactive Chat Flow](#interactive-chat-flow)
4. [Web Search Flow](#web-search-flow)
5. [Vision Analysis Flow](#vision-analysis-flow)
6. [Embeddings Flow](#embeddings-flow)
7. [Model Listing Flow](#model-listing-flow)
8. [Retry Logic Flow](#retry-logic-flow)
9. [Error Handling Flow](#error-handling-flow)

---

## Application Startup Flow

```
┌─────────────────┐
│   User Shell    │
└────────┬────────┘
         │ ./syn [args]
         ▼
┌─────────────────────────────────────────────────────┐
│                    main.go                          │
│  ┌──────────────────────────────────────────────┐  │
│  │  config.SetDefaults()                        │  │
│  │  - Sets viper defaults (API URL, model, etc)│  │
│  │  - WARNING: Hardcoded API key here 🚨       │  │
│  └──────────────┬───────────────────────────────┘  │
│                 ▼                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │  cmd.Execute()                               │  │
│  └──────────────┬───────────────────────────────┘  │
└─────────────────┼───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│                 cmd/root.go                         │
│  ┌──────────────────────────────────────────────┐  │
│  │  rootCmd.PersistentPreRunE()                 │  │
│  │  ├─ Skip for: completion, help, version      │  │
│  │  └─ Call initConfig()                        │  │
│  │     ├─ Load config file if specified         │  │
│  │     ├─ Check ~/.config/syn/config.yaml       │  │
│  │     ├─ Set env prefix: SYN_*                 │  │
│  │     └─ Validate API key present              │  │
│  └──────────────┬───────────────────────────────┘  │
│                 ▼                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │  Route to subcommand:                        │  │
│  │  ├─ No args → help                           │  │
│  │  ├─ "chat" → chatCmd                         │  │
│  │  ├─ "search" → searchCmd                     │  │
│  │  ├─ "vision" → visionCmd                     │  │
│  │  ├─ "embed" → embedCmd                       │  │
│  │  ├─ "model" → modelCmd                       │  │
│  │  └─ Args → runOneShot()                      │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

---

## One-Shot Chat Flow

```
User: ./syn -f main.go "Explain this code"

┌────────────────────────────────────────────────────┐
│              cmd/root.go: RunE()                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Check stdin (hasStdinData)              │  │
│  │     ├─ Yes: readStdin() → stdinData         │  │
│  │     └─ No: stdinData = ""                   │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Build prompt from args                  │  │
│  │     prompt = strings.Join(args, " ")        │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Combine prompt + stdin                  │  │
│  │     if stdinData != "":                     │  │
│  │       prompt += "\n<stdin>\n" + data        │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  4. Call runOneShot(prompt)                 │  │
│  └─────────────┬───────────────────────────────┘  │
└────────────────┼────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│          cmd/root.go: runOneShot()                 │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Create client                           │  │
│  │     client = newClient()                    │  │
│  │     └─ buildClientConfig()                  │  │
│  │        ├─ Read viper settings               │  │
│  │        └─ Build RetryConfig                 │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Build options                           │  │
│  │     opts = DefaultChatOptions()             │  │
│  │     opts.FilePath = viper.GetString("file") │  │
│  │     opts.Model = viper.GetString("model")   │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Call API                                │  │
│  │     ctx, cancel = context.WithTimeout(5m)  │  │
│  │     response, err = client.Chat(ctx,        │  │
│  │                                  prompt,     │  │
│  │                                  opts)       │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  4. Output                                  │  │
│  │     if --json:                              │  │
│  │       Marshal to JSON                       │  │
│  │     else:                                   │  │
│  │       fmt.Println(response)                 │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│         internal/app/client.go: Chat()             │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Validate API key                        │  │
│  │     requireAPIKey()                         │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Build content                           │  │
│  │     buildContent(prompt, filePath)          │  │
│  │     └─ If file:                             │  │
│  │        ├─ Read file (os.ReadFile)           │  │
│  │        └─ Append to prompt                  │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Build messages                          │  │
│  │     buildMessagesWithContext(content, opts) │  │
│  │     ├─ System: "Be concise..."              │  │
│  │     ├─ Previous context (if any)            │  │
│  │     └─ User: content                        │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  4. Execute with retry                      │  │
│  │     doRequestWithRetry(ctx, messages, opts) │  │
│  │     (See Retry Logic Flow below)            │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  5. Return response                         │  │
│  │     return response.Content, nil            │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

## Interactive Chat Flow

```
User: ./syn chat

┌────────────────────────────────────────────────────┐
│         cmd/chat.go: runInteractiveChat()          │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Setup                                   │  │
│  │     ctx, stop = signal.NotifyContext()      │  │
│  │     client = newClient()                    │  │
│  │     conversationContext = []Message{}       │  │
│  │     maxContextMessages = 20                 │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Show welcome banner                     │  │
│  │     printWelcomeBanner()                    │  │
│  │     ├─ Title: "SYN Chat Session"            │  │
│  │     ├─ Model name                           │  │
│  │     └─ Help: /help, /clear, etc             │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Main loop                               │  │
│  │     for {                                   │  │
│  └──────┬──────────────────────────────────────┘  │
│         │                                          │
│         ▼                                          │
│  ┌─────────────────────────────────────────────┐  │
│  │  4. Read input (async)                      │  │
│  │     go func() {                             │  │
│  │       if scanner.Scan() {                   │  │
│  │         inputCh <- text                     │  │
│  │       }                                     │  │
│  │     }()                                     │  │
│  │     ⚠️ ISSUE: Goroutine leak on Ctrl-C     │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  5. Wait for input or cancellation          │  │
│  │     select {                                │  │
│  │       case <-ctx.Done():                    │  │
│  │         → Exit gracefully                   │  │
│  │       case result := <-inputCh:             │  │
│  │         → Process input                     │  │
│  │     }                                       │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  6. Handle commands                         │  │
│  │     if strings.HasPrefix(input, "/"):       │  │
│  │       handleChatCommand(input, &context)    │  │
│  │       ├─ /clear → Reset context             │  │
│  │       ├─ /model → Show current model        │  │
│  │       ├─ /exit → os.Exit(0)                 │  │
│  │       ├─ /help → Show help                  │  │
│  │       └─ /context → Show history            │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  7. Build options with context              │  │
│  │     opts = baseOpts                         │  │
│  │     opts.Context = conversationContext      │  │
│  │     if len(context) > 0:                    │  │
│  │       opts.FilePath = ""  # Only first msg  │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  8. Show spinner                            │  │
│  │     var spinnerStop atomic.Bool             │  │
│  │     go animateThinking(nil, &spinnerStop)   │  │
│  │     (Braille spinner: ⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏)          │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  9. Send to API                             │  │
│  │     response, err = client.Chat(ctx,        │  │
│  │                                  input,      │  │
│  │                                  opts)       │  │
│  │     spinnerStop.Store(true)                 │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  10. Update context                         │  │
│  │      conversationContext += [               │  │
│  │        {role: "user", content: input},      │  │
│  │        {role: "assistant", content: resp}   │  │
│  │      ]                                      │  │
│  │      if len(context) > 20:                  │  │
│  │        context = context[2:]  # Trim old    │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  11. Display response                       │  │
│  │      fmt.Printf("syn> %s\n", response)      │  │
│  │      (Back to step 3)                       │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

**Context Window Management:**

```
Message 1  ──┐
Message 2    │
Message 3    │
...          ├─ Keep last 20 messages
Message 18   │  (10 user + 10 assistant)
Message 19   │
Message 20 ──┘
Message 21 ──┐
Message 22 ──┘ Discarded (oldest first)
```

---

## Web Search Flow

```
User: ./syn search -i "golang patterns"

┌────────────────────────────────────────────────────┐
│          cmd/search.go: runSearch()                │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Parse query                             │  │
│  │     ├─ From args (priority)                 │  │
│  │     └─ From stdin (fallback)                │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Create client & call API                │  │
│  │     client = newClient()                    │  │
│  │     ctx, cancel = context.WithTimeout(30s) │  │
│  │     resp, err = client.Search(ctx, query)   │  │
│  └─────────────┬───────────────────────────────┘  │
└────────────────┼────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│        internal/app/client.go: Search()            │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Validate                                │  │
│  │     requireAPIKey()                         │  │
│  │     if query == "": return error            │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Build request                           │  │
│  │     reqData = SearchRequest{Query: query}   │  │
│  │     jsonData = json.Marshal(reqData)        │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Construct URL                           │  │
│  │     ⚠️ FRAGILE:                             │  │
│  │     url = strings.Replace(baseURL,          │  │
│  │                           "/openai/v1",     │  │
│  │                           "/v2/search", 1)  │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  4. Send HTTP POST                          │  │
│  │     req = NewRequest(POST, url, jsonData)   │  │
│  │     req.Header.Set("Authorization", key)    │  │
│  │     resp, err = httpClient.Do(req)          │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  5. Parse response                          │  │
│  │     json.Unmarshal(body, &SearchResponse)   │  │
│  │     return searchResp.Results, nil          │  │
│  └─────────────────────────────────────────────┘  │
└────────────────┬───────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│        cmd/search.go: Output Results               │
│  ┌─────────────────────────────────────────────┐  │
│  │  if --json:                                 │  │
│  │    Marshal response to JSON                 │  │
│  │  else:                                      │  │
│  │    printSearchResults(resp)                 │  │
│  │    ├─ Show styled results                   │  │
│  │    ├─ Title, URL, Snippet                   │  │
│  │    └─ Published date                        │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  if -i (interactive):                       │  │
│  │    interactiveSelection(results)            │  │
│  │    ┌────────────────────────────────────┐   │  │
│  │    │  Loop:                             │   │  │
│  │    │    Prompt: "Select (1-N, q):"      │   │  │
│  │    │    Read input                      │   │  │
│  │    │    if 'q': exit                    │   │  │
│  │    │    Parse number                    │   │  │
│  │    │    openBrowser(results[n].URL)     │   │  │
│  │    │    ├─ macOS: open <url>            │   │  │
│  │    │    ├─ Linux: xdg-open <url>        │   │  │
│  │    │    └─ Windows: rundll32 ...        │   │  │
│  │    └────────────────────────────────────┘   │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

## Vision Analysis Flow

```
User: ./syn vision photo.jpg "What's in this image?"

┌────────────────────────────────────────────────────┐
│           cmd/vision.go: runVision()               │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Parse args                              │  │
│  │     imageSource = args[0]                   │  │
│  │     prompt = args[1:] or default            │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Call API                                │  │
│  │     client = newClient()                    │  │
│  │     response, err = client.Vision(ctx,      │  │
│  │                                   prompt,    │  │
│  │                                   imageSource,│  │
│  │                                   opts)      │  │
│  └─────────────┬───────────────────────────────┘  │
└────────────────┼────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│        internal/app/client.go: Vision()            │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Validate API key                        │  │
│  │     requireAPIKey()                         │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Process image source                    │  │
│  │     if http(s) URL:                         │  │
│  │       imageURL = imageSource                │  │
│  │     else:  # Local file                     │  │
│  │       ⚠️ NO SIZE CHECK                      │  │
│  │       data = os.ReadFile(imageSource)       │  │
│  │       Detect MIME from extension:           │  │
│  │         .png  → image/png                   │  │
│  │         .jpg  → image/jpeg                  │  │
│  │         .gif  → image/gif                   │  │
│  │         .webp → image/webp                  │  │
│  │       imageURL = "data:{mime};base64,{b64}" │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Resolve vision model                    │  │
│  │     model = ResolveModel("qwen")            │  │
│  │     → "hf:Qwen/Qwen3-VL-235B-A22B-Instruct" │  │
│  │     (Override if opts.Model set)            │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  4. Build multimodal content                │  │
│  │     content = [                             │  │
│  │       {                                     │  │
│  │         "type": "image_url",                │  │
│  │         "image_url": {"url": imageURL}      │  │
│  │       },                                    │  │
│  │       {                                     │  │
│  │         "type": "text",                     │  │
│  │         "text": prompt                      │  │
│  │       }                                     │  │
│  │     ]                                       │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  5. Build request                           │  │
│  │     reqData = {                             │  │
│  │       "model": model,                       │  │
│  │       "messages": [{                        │  │
│  │         "role": "user",                     │  │
│  │         "content": content                  │  │
│  │       }],                                   │  │
│  │       "max_tokens": 4096,                   │  │
│  │       "temperature": opts.Temperature       │  │
│  │     }                                       │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  6. Send HTTP POST                          │  │
│  │     url = baseURL + "/chat/completions"     │  │
│  │     req = NewRequest(POST, url, json)       │  │
│  │     resp, err = httpClient.Do(req)          │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  7. Parse response                          │  │
│  │     json.Unmarshal(body, &ChatResponse)     │  │
│  │     return choices[0].message.content       │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

## Embeddings Flow

```
User: ./syn embed "Hello world" "Goodbye world"

┌────────────────────────────────────────────────────┐
│           cmd/embed.go: runEmbed()                 │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Collect texts                           │  │
│  │     ├─ From stdin (if available)            │  │
│  │     └─ From args                            │  │
│  │     texts = ["Hello world", "Goodbye..."]   │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Call API                                │  │
│  │     client = newClient()                    │  │
│  │     model = viper.GetString("embedding...")│  │
│  │     resp, err = client.Embed(ctx, texts,    │  │
│  │                               model)        │  │
│  └─────────────┬───────────────────────────────┘  │
└────────────────┼────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│         internal/app/client.go: Embed()            │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Validate                                │  │
│  │     requireAPIKey()                         │  │
│  │     if len(texts) == 0: return error        │  │
│  │     ⚠️ NO ARRAY SIZE LIMIT                  │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Build request                           │  │
│  │     reqData = EmbeddingRequest{             │  │
│  │       Model: model,                         │  │
│  │       Input: texts,                         │  │
│  │     }                                       │  │
│  │     jsonData = json.Marshal(reqData)        │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Send HTTP POST                          │  │
│  │     url = baseURL + "/embeddings"           │  │
│  │     req = NewRequest(POST, url, jsonData)   │  │
│  │     req.Header.Set("Authorization", key)    │  │
│  │     resp, err = httpClient.Do(req)          │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  4. Parse response                          │  │
│  │     json.Unmarshal(body, &EmbeddingResp)    │  │
│  │     return embedResp, nil                   │  │
│  └─────────────────────────────────────────────┘  │
└────────────────┬───────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│           cmd/embed.go: Output                     │
│  ┌─────────────────────────────────────────────┐  │
│  │  if --json:                                 │  │
│  │    Marshal full response to JSON            │  │
│  │  else:                                      │  │
│  │    Print human-readable:                    │  │
│  │    ├─ Model name                            │  │
│  │    ├─ Total tokens                          │  │
│  │    └─ For each embedding:                   │  │
│  │       ├─ Original text (truncated)          │  │
│  │       ├─ Dimensions (e.g., 768)             │  │
│  │       └─ First 5 values preview             │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

**Example Output:**
```
Embeddings (2)
──────────────────────────────────────────────────
  Model: hf:nomic-ai/nomic-embed-text-v1.5
  Total tokens: 12

  Text 1: Hello world
    Dimensions: 768
    First 5: [0.123, -0.456, 0.789, 0.012, -0.345]

  Text 2: Goodbye world
    Dimensions: 768
    First 5: [0.234, -0.567, 0.890, 0.123, -0.456]
```

---

## Model Listing Flow

```
User: ./syn model list

┌────────────────────────────────────────────────────┐
│        cmd/model.go: modelListCmd.RunE()           │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Create client                           │  │
│  │     client = newClient()                    │  │
│  │     ctx, cancel = context.WithTimeout(30s) │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Call API                                │  │
│  │     models, err = client.ListModels(ctx)    │  │
│  └─────────────┬───────────────────────────────┘  │
└────────────────┼────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│      internal/app/client.go: ListModels()          │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Validate API key                        │  │
│  │     requireAPIKey()                         │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Send HTTP GET                           │  │
│  │     url = baseURL + "/models"               │  │
│  │     req = NewRequest(GET, url, nil)         │  │
│  │     req.Header.Set("Authorization", key)    │  │
│  │     resp, err = httpClient.Do(req)          │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  3. Parse response                          │  │
│  │     json.Unmarshal(body, &ModelsResponse)   │  │
│  │     return modelsResp.Data, nil             │  │
│  └─────────────────────────────────────────────┘  │
└────────────────┬───────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│           cmd/model.go: Display                    │
│  ┌─────────────────────────────────────────────┐  │
│  │  For each model:                            │  │
│  │    Print styled:                            │  │
│  │    ├─ Model ID (command style)              │  │
│  │    ├─ Owner (if present)                    │  │
│  │    └─ Created date (if present)             │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

## Retry Logic Flow

```
┌────────────────────────────────────────────────────┐
│   internal/app/client.go: doRequestWithRetry()     │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. Initialize retry state                  │  │
│  │     maxAttempts = config.MaxAttempts (3)    │  │
│  │     initialBackoff = config.Initial (1s)    │  │
│  │     maxBackoff = config.Max (30s)           │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  2. Attempt loop (for 1..maxAttempts)       │  │
│  │     ┌─────────────────────────────────────┐ │  │
│  │     │  A. Check context cancellation      │ │  │
│  │     │     select { case <-ctx.Done(): }   │ │  │
│  │     │     → return ctx.Err() if cancelled │ │  │
│  │     └─────────────┬───────────────────────┘ │  │
│  │                   ▼                         │  │
│  │     ┌─────────────────────────────────────┐ │  │
│  │     │  B. Calculate backoff (if retry)    │ │  │
│  │     │     if attempt > 1:                 │ │  │
│  │     │       backoff = calculateBackoff(   │ │  │
│  │     │         attempt,                    │ │  │
│  │     │         initialBackoff,             │ │  │
│  │     │         maxBackoff)                 │ │  │
│  │     │       ┌─────────────────────────┐   │ │  │
│  │     │       │  Exponential + Jitter   │   │ │  │
│  │     │       │  ───────────────────────│   │ │  │
│  │     │       │  base = initial * 2^n   │   │ │  │
│  │     │       │  cap at maxBackoff      │   │ │  │
│  │     │       │  jitter = ±12.5%        │   │ │  │
│  │     │       │  return base + jitter   │   │ │  │
│  │     │       └─────────────────────────┘   │ │  │
│  │     │       Sleep(backoff)                │ │  │
│  │     └─────────────┬───────────────────────┘ │  │
│  │                   ▼                         │  │
│  │     ┌─────────────────────────────────────┐ │  │
│  │     │  C. Execute request                 │ │  │
│  │     │     response, usage, err =          │ │  │
│  │     │       doRequest(ctx, messages, opts)│ │  │
│  │     └─────────────┬───────────────────────┘ │  │
│  │                   ▼                         │  │
│  │     ┌─────────────────────────────────────┐ │  │
│  │     │  D. Check result                    │ │  │
│  │     │     if err == nil:                  │ │  │
│  │     │       → return response ✅          │ │  │
│  │     └─────────────┬───────────────────────┘ │  │
│  │                   ▼                         │  │
│  │     ┌─────────────────────────────────────┐ │  │
│  │     │  E. Check if retryable              │ │  │
│  │     │     ⚠️ STRING MATCHING:             │ │  │
│  │     │     isRetryable = contains(         │ │  │
│  │     │       err.Error(),                  │ │  │
│  │     │       ["429", "503", "timeout", ...]│ │  │
│  │     │     )                               │ │  │
│  │     │     if !isRetryable:                │ │  │
│  │     │       → return error immediately    │ │  │
│  │     │     if attempt == maxAttempts:      │ │  │
│  │     │       → return error (gave up)      │ │  │
│  │     └─────────────┬───────────────────────┘ │  │
│  │                   │ (Continue loop)         │  │
│  │                   └─────────────────────────┘ │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

**Backoff Timeline Example:**

```
Attempt 1: Immediate (0s)
   ↓ Error 429
Attempt 2: Wait 1s + jitter (-0.125s to +0.125s)
   ↓ Error 503
Attempt 3: Wait 2s + jitter (-0.25s to +0.25s)
   ↓ Success ✅
Return response

OR

Attempt 1: Immediate (0s)
   ↓ Error 404 (not retryable)
Return error immediately ❌
```

---

## Error Handling Flow

```
┌────────────────────────────────────────────────────┐
│                 Error Sources                      │
│  ┌─────────────────────────────────────────────┐  │
│  │  1. API Errors (non-200 response)           │  │
│  │  2. Network Errors (timeout, refused)       │  │
│  │  3. Validation Errors (missing key, file)   │  │
│  │  4. Context Errors (cancellation, timeout)  │  │
│  │  5. Parsing Errors (invalid JSON)           │  │
│  └─────────────┬───────────────────────────────┘  │
└────────────────┼────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│            Error Classification                    │
│  ┌─────────────────────────────────────────────┐  │
│  │  if resp.StatusCode != 200:                 │  │
│  │    → return &APIError{                      │  │
│  │        StatusCode: code,                    │  │
│  │        Body: body                           │  │
│  │      }                                      │  │
│  └─────────────┬───────────────────────────────┘  │
│                ▼                                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  Error Wrapping Chain:                      │  │
│  │  ┌─────────────────────────────────────┐    │  │
│  │  │  Level 1: HTTP Layer                │    │  │
│  │  │  "failed to send request: %w"       │    │  │
│  │  └─────────────┬───────────────────────┘    │  │
│  │                ▼                             │  │
│  │  ┌─────────────────────────────────────┐    │  │
│  │  │  Level 2: Client Layer              │    │  │
│  │  │  "chat failed: %w"                  │    │  │
│  │  └─────────────┬───────────────────────┘    │  │
│  │                ▼                             │  │
│  │  ┌─────────────────────────────────────┐    │  │
│  │  │  Level 3: Command Layer             │    │  │
│  │  │  "failed to get response: %w"       │    │  │
│  │  └─────────────┬───────────────────────┘    │  │
│  │                ▼                             │  │
│  │  ┌─────────────────────────────────────┐    │  │
│  │  │  Level 4: Root Handler              │    │  │
│  │  │  Display styled error               │    │  │
│  │  │  os.Exit(1)                         │    │  │
│  │  └─────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

**Error Display:**

```
┌────────────────────────────────────────────────────┐
│           cmd/root.go: Execute()                   │
│  ┌─────────────────────────────────────────────┐  │
│  │  if err := rootCmd.Execute(); err != nil {  │  │
│  │    fmt.Fprintf(os.Stderr,                   │  │
│  │      "\n%s %s\n\n",                         │  │
│  │      theme.ErrorText.Render("Error:"),      │  │
│  │      theme.Description.Render(err.Error())) │  │
│  │    os.Exit(1)                               │  │
│  │  }                                          │  │
│  └─────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

**Terminal Output:**
```
 Error:  failed to get response: chat failed: API error: 429 - Rate limit exceeded
```

**Wrapped Error Stack:**
```
APIError{StatusCode: 429, Body: "Rate limit exceeded"}
  ↑
fmt.Errorf("chat failed: %w", apiErr)
  ↑
fmt.Errorf("failed to get response: %w", err)
  ↑
Display to user
```

---

## Data Flow Summary

### Chat Request Lifecycle

```
User Input
    ↓
┌───────────────┐
│ CLI Parser    │ (Cobra: args, flags, stdin)
└───────┬───────┘
        ↓
┌───────────────┐
│ Config Loader │ (Viper: file, env, defaults)
└───────┬───────┘
        ↓
┌───────────────┐
│ Client Factory│ (Build ClientConfig + RetryConfig)
└───────┬───────┘
        ↓
┌───────────────┐
│ Message Build │ (System + Context + User)
└───────┬───────┘
        ↓
┌───────────────┐
│ Retry Loop    │ (Exponential backoff + jitter)
└───────┬───────┘
        ↓
┌───────────────┐
│ HTTP Request  │ (POST /chat/completions)
└───────┬───────┘
        ↓
┌───────────────┐
│ API Response  │ (JSON: choices, usage, etc)
└───────┬───────┘
        ↓
┌───────────────┐
│ Parse + Return│ (Extract content)
└───────┬───────┘
        ↓
┌───────────────┐
│ Output Format │ (Plain text or JSON)
└───────────────┘
```

---

## Configuration Priority

```
Priority (highest to lowest):
1. Command-line flags (--model kimi)
2. Environment variables (SYN_API_KEY)
3. Config file (~/.config/syn/config.yaml)
4. Viper defaults (config.SetDefaults)
```

**Example Resolution:**

```
Model Selection:
┌──────────────────────────────────────┐
│ --model flag set?                    │
│ ├─ Yes: Use flag value               │
│ └─ No: Check viper                   │
│    ├─ SYN_MODEL env set?             │
│    │  ├─ Yes: Use env value          │
│    │  └─ No: Check config file       │
│    │     ├─ model: key in YAML?      │
│    │     │  ├─ Yes: Use config value │
│    │     │  └─ No: Use default       │
│    │     │     → "hf:deepseek-ai/..." │
│    │     │                            │
│    └──> Pass through ResolveModel()  │
│         ├─ Alias? Return full name   │
│         └─ Not alias? Return as-is   │
└──────────────────────────────────────┘
```

---

## Concurrency Patterns

### Chat Session Goroutines

```
Main Thread                 Input Goroutine        Spinner Goroutine
     │                             │                      │
     ├─ spawn ─────────────────────►                      │
     │  (scanner.Scan)              │                      │
     │                              │                      │
     ├─ spawn ──────────────────────┼──────────────────────►
     │  (animateThinking)           │                      │
     │                              │                      │
     ├─ select {                    │                      │
     │   case <-ctx.Done():         │                      │
     │     return  ─────────────────┼──────────────────────┤ ⚠️ LEAK!
     │   case input := <-inputCh:   │                      │
     │     ◄────────────────────────┤                      │
     │     process...               │                      │
     │     ├─ Send API request      │                      │
     │     ├─ Stop spinner ─────────┼──────────────────────►
     │     │  (atomic.Store)        │                      │
     │     └─ Update context        │                      │
     │ }                            │                      │
     └──────────────────────────────┴──────────────────────┘
```

**Issue:** Input goroutine not cancelled on context done.

**Fix:** Use `errgroup.WithContext`

---

## Performance Characteristics

### Latency Breakdown (Estimated)

```
One-Shot Chat Request (end-to-end):
┌─────────────────────────────────────┐
│ Config load      │  50ms             │
│ Client init      │  10ms             │
│ File read (if)   │ 100ms (10MB)      │
│ Message build    │  5ms              │
│ HTTP connect     │ 100ms             │
│ API processing   │ 2-10s (varies)    │
│ Response parse   │  10ms             │
│ Display          │  5ms              │
├─────────────────────────────────────┤
│ Total            │ 2.3s - 10.3s      │
└─────────────────────────────────────┘
```

### Memory Usage

```
Component              Peak Memory
────────────────────────────────────
Client struct          ~1 KB
Message array (20)     ~50 KB
File buffer (10MB)     10 MB
HTTP response (8K)     ~20 KB
JSON parsing           ~100 KB
────────────────────────────────────
Typical Total          ~11 MB
```

---

This flow documentation provides a comprehensive view of all request/response paths through the SYN CLI codebase.
